server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/nbb-template-system?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true&rewriteBatchedStatements=true
    username: root
    password: root
  redis:
    host: localhost
    port: 6379
    database: 10
    password: root

logging:
  file:
    name: nbb-template-admin
    path: d:/logs/

